Для разгортвання праекта лакальна
дастаткова выканаць `npm install` ці `yarn install`
ды запусціць праект - `npm run start` ці `yarn start`

Для зборкі праекта выканайце `npm run build` ці `yarn run build`
